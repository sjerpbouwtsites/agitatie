{
  "name": "agitatie",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "rollup-watch": "rollup --config resources/rollup.config.js -w --bundleConfigAsCjs",
    "rollup-productie": "rollup --config resources/rollup.config.js --bundleConfigAsCjs --compact",
    "alles-productie": "cd ../; cd hoofdthema; npm run rollup-productie; cd ../; cd kindthema; npm run rollup-productie",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup-plugin-scss": "^2.6.1"
  },
  "dependencies": {
    "node-sass": "^9.0.0"
  }
}
